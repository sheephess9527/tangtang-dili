const data=window.GEO_UNITS_DATA;const stats=GeoMastery.stats(data);const built=GeoTaskEngine.buildTasks(data);document.getElementById('doneQ').textContent=stats.done;document.getElementById('doneU').textContent=stats.completedUnits;document.getElementById('doneT').textContent=stats.doneTasks;document.getElementById('taskSummary').innerHTML=built.tasks.length?built.tasks.slice(0,3).map(t=>`<article class="detail-card"><div class="quiz-meta"><span class="tag">${t.type}</span></div><h3>${t.title}</h3><p style="margin:0;color:var(--muted);line-height:1.8">${t.desc}</p><p style="margin-top:14px"><a class="btn secondary" href="${t.link}">去完成</a></p></article>`).join(''):'<article class="detail-card"><h3>当前暂无任务</h3><p style="margin:0;color:var(--muted)">继续去题库做题，系统会自动生成任务。</p></article>';