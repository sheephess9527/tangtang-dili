const data=window.GEO_UNITS_DATA;const list=document.getElementById('quizList');const search=document.getElementById('quizSearch');const book=document.getElementById('quizBook');const type=document.getElementById('quizType');const source=document.getElementById('quizSource');const tag=document.getElementById('quizTag');data.books.forEach(b=>{const opt=document.createElement('option');opt.value=b.id;opt.textContent=b.name;book.appendChild(opt);});function render(){const keyword=(search.value||'').trim().toLowerCase();const bookId=book.value;const typeVal=type.value;const sourceVal=source.value;const tagVal=(tag.value||'').trim().toLowerCase();const store=GeoMastery.get();const items=data.question_bank.filter(q=>{const unit=data.all_units.find(u=>u.id===q.unit_id);const hay=[q.stem,q.answer,q.explanation,...(q.options||[]),...(q.syllabus_tags||[]),q.textbook_unit||'',q.training_layer||''].join(' ').toLowerCase();const normalized=q.is_real_exam?'真题':q.source_type;return (!keyword||hay.includes(keyword))&&(bookId==='ALL'||(unit&&unit.book_id===bookId))&&(typeVal==='ALL'||q.type===typeVal)&&(sourceVal==='ALL'||normalized===sourceVal)&&(!tagVal||(q.syllabus_tags||[]).join(' ').toLowerCase().includes(tagVal));});list.innerHTML=items.length?items.map((q,i)=>{const done=store.doneQuestions.has(q.id);const wrong=store.wrongQuestions.has(q.id);return `<article class="quiz-card"><div class="quiz-meta"><span class="tag">${q.type}</span><span class="tag">${q.is_real_exam?'真题':q.source_type}</span><span class="tag">${q.training_layer||q.paper||q.year||'通用'}</span>${done?'<span class="tag">已完成</span>':''}${wrong?'<span class="tag">错题</span>':''}</div><h3>${q.stem}</h3><p style="margin:0 0 10px;color:var(--muted)">教材单元：${q.textbook_unit}；考点：${(q.syllabus_tags||[]).join('、')||'未标注'}</p>${q.options&&q.options.length?`<ul>${q.options.map(o=>`<li>${o}</li>`).join('')}</ul>`:''}<div class="quiz-actions"><button class="answer-btn" data-a="${i}">查看答案与解析</button><button class="mini-btn secondary" data-d="${q.id}">${done?'取消完成':'标记完成'}</button><button class="mini-btn secondary" data-w="${q.id}" data-tags="${(q.syllabus_tags||[]).join('|')}">${wrong?'取消错题':'加入错题'}</button></div><div class="quiz-answer hidden" id="ans-${i}"><strong>答案：</strong>${q.answer}<br><strong>解析：</strong>${q.explanation}<br><strong>题源：</strong>${q.is_real_exam?'真题':q.source_type}；${q.source_note}</div></article>`}).join(''):'<div class="empty panel" style="grid-column:1/-1">当前筛选结果下没有题目。</div>';document.querySelectorAll('[data-a]').forEach(btn=>btn.onclick=()=>document.getElementById('ans-'+btn.dataset.a).classList.toggle('hidden'));document.querySelectorAll('[data-d]').forEach(btn=>btn.onclick=()=>{GeoMastery.toggleDone(btn.dataset.d);render();});document.querySelectorAll('[data-w]').forEach(btn=>btn.onclick=()=>{GeoMastery.toggleWrong(btn.dataset.w,(btn.dataset.tags||'').split('|').filter(Boolean));render();});}search.oninput=render;book.onchange=render;type.onchange=render;source.onchange=render;tag.oninput=render;render();